{% if _navbar-sub-links < 2 %}
  {% assign i = forloop.index %}
  {% assign submenu = ll.title | handleize %}
  {% assign show = true %}
  {% assign min_products = settings.hide-products-with-less-than-this-number-of-products | plus: 0 %}
  {% if ll.type == 'collection_link' and ll.object.all_products.size <= min_products %}{% assign show = false %}{% endif %}
  {% if linklists[submenu].links.size > 0 and _navbar-sub-links < 1 and show %}
    <li class="submenu">
      <ul class="nav">
        <li class="dropdown-header"><a tabindex="-1" href="{{ ll.url }}" title="{{ ll.title }}">{{ ll.title }} - {{ i }}</a></li>
        {% for lll in linklists[submenu].links %}
          {% assign show = true %}
          {% if lll.type == 'collection_link' and lll.object.all_products.size <= min_products %}{% assign show = false %}{% endif %}
          {% if show %}
          {% assign level_count = _navbar-sub-links | plus: 1 %}
          {% include '_navbar-sub-links' with level_count %}
          {% endif %}
		  {% if lll.active %}{% assign current_list = l.handle %}{% endif %}
        {% endfor %}
      </ul>
    </li>
  {% else %}
    <li><a tabindex="-1" href="{{ ll.url }}" title="{{ ll.title }}">{{ ll.title }} - {{ i }}</a></li>
  {% endif %}
{% endif %}
{% if ll.active %}{% assign current_list = l.handle %}{% endif %}

