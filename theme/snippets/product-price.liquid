{% if product-price.price_varies %}<small class="muted">{% include '__localize' with 'from' %}&nbsp;</small><span class='money'>{{ product-price.price_min | money_with_currency }}</span>{% else %}<span class='money'>{{ product-price.price | money_with_currency }}</span>{% endif %}{% if product-price.compare_at_price_min > 0 %} <span class="label label-success">{% include '__localize' with 'On sale' %}</span>{% endif %}