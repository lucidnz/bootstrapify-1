 data-variants="{% for variant in _product.variants %}{% if variant.available == true %}{{ variant.title | downcase | handleize }} {% endif %}{% endfor %}" data-alpha="{{_product.title}}" data-price="{{_product.price}}"