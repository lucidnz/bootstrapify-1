<section id="404" class="section-wrap">
	{% include '__containers-open' %}
	<h1 class="page-header">{{ page_title }}</h1>

	<p class="lead">{% include '__localize' with 'Apologies! The page could not be found.' %}</p>
	{% include '__containers-close' %}
</section>