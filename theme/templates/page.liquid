{% include '__has-variables' %}
<section id="page-wrap" class="section-wrap">
	<h1 class="page-header">{{ page_title }}</h1>

	<p>{% include '__offset-centered-content' with page.content %}</p>
</section>